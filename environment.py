def before_all(context):
    # Setup is now handled dynamically inside the step definitions.
    # We leave this hook empty for now, but you can use it later 
    # for global teardowns or starting up a test database!
    pass